namespace GameNetworking.Messages {
    public enum MessageType {
        SPAWN_REQUEST = 100,
        CONNECTED_PLAYER,
        SYNC,
        START_GAME,
        PING,
        PONG,
        PING_RESULT
    }
}