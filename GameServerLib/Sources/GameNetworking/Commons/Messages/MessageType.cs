namespace GameNetworking.Messages {
    public enum MessageType {
        CONNECTED_PLAYER = 100,
        DISCONNECTED_PLAYER,
        START_GAME,
        PING,
        PONG,
        PING_RESULT
    }
}